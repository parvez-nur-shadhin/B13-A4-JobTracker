function getElementValues(id){
    const element = document.getElementById(id);
    const values = element.innerText;
    return values;
}
function getElementOnly(id){
    const element = document.getElementById(id);
    return element;
}
